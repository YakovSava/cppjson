# include <string>
using namespace std;

# pragma once
namespace JSONObjects {
	string null = "null";
	string apostrof = "'";
}

# pragma once
namespace PythonObjects {
	string null = "None";
	string apostrof = "\"";
}